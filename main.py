from config.main_cfg import MainConfig
from tqdm.auto import trange
import matplotlib.pyplot as plt


def main(cfg: MainConfig) -> None:
    pass


if __name__ == "__main__":
    cfg = MainConfig()
    main(cfg)
